<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.al.nppm.business.cpcp.dao.CpcMapper" >

    <!--赠款目录-1064785 、红包金目录-1064786-->
    <select id="getCountFromOfferCatalogLocation" resultType="java.util.HashMap" parameterType="map" >
    select  CATALOG_ITEM_ID catalogItemId,OFFER_ID offerId
    from OFFER_CATALOG_LOCATION
	where CATALOG_ITEM_ID in( '-1064785','-1064786')
           AND OFFER_ID = #{offerId}
  </select>
    <!--红包金目录-->
    <select id="getCountFromOfferCatalogLocation_hongbao" resultType="java.lang.Long" parameterType="map" >
    select  count(*)
    from OFFER_CATALOG_LOCATION
	where CATALOG_ITEM_ID = '-1064786'
           AND OFFER_ID = #{offerId}
  </select>

    <select id="getCountFromPOfferPayplanInfo" resultType="java.lang.Long" parameterType="map" >
    select  count(*)
    from P_OFFER_PAYPLAN_INFO
	where  OFFER_ID = #{offerId}
  </select>

    <select id="getShareLevel" resultType="java.lang.Long" parameterType="map" >
    select  min(SHARE_LEVEL)
    from P_OFFER_PAYPLAN_INFO
	where  OFFER_ID = #{offerId}
  </select>
    <select id="selectPOfferPayplanInfo" resultType="java.util.HashMap" parameterType="map" >
    select confer_flag conferFlag
    from P_OFFER_PAYPLAN_INFO
	where  OFFER_ID = #{offerId}
  </select>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.al.nppm.business.account.dao.CpcMapper" >

    <select id="getCountFromOfferCatalogLocation" resultType="java.lang.Long" parameterType="map" >
    select  count(*)
    from OFFER_CATALOG_LOCATION
	where CATALOG_ITEM_ID = '-1040002'
           AND OFFER_ID = #{offerId}
  </select>

    <select id="getCountFromPOfferPayplanInfo" resultType="java.lang.Long" parameterType="map" >
    select  count(*)
    from P_OFFER_PAYPLAN_INFO
	where  OFFER_ID = #{offerId}
  </select>

    <select id="getShareLevel" resultType="java.lang.Long" parameterType="map" >
    select  min(SHARE_LEVEL)
    from P_OFFER_PAYPLAN_INFO
	where  OFFER_ID = #{offerId}
  </select>

</mapper>